Step:

1) Type "module load mpich-3.0.4/gcc-4.8.3" to run the module to allow you to compile the Makefile
2) Run "make"
3) Use the script in HPC Cluster and run it there using the commands
4) Send the batch job and then check the results